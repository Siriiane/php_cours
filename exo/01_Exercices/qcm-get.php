<?php 

/*
QCM : Méthode GET en HTTP

Répondre par oui ou non :

La méthode GET est utilisée pour récupérer des données depuis un serveur.
-->oui

Les paramètres d'une requête GET sont envoyés dans le corps de la requête.

-->oui
Les paramètres de la méthode GET sont visibles dans l'URL.
oui

La méthode GET est sécurisée pour transmettre des informations sensibles comme des mots de passe.
-->non

Les données transmises via GET peuvent être mises en cache par le navigateur.

-->oui
Les requêtes GET sont souvent utilisées pour des opérations qui ne modifient pas l'état du serveur.
non

Il est possible d'envoyer des fichiers via une requête GET.
-->oui

La méthode GET est généralement utilisée pour récupérer des ressources statiques comme des images ou des fichiers CSS.
-->non

Les requêtes GET sont limitées en taille par la longueur de l'URL.
-->non

Une requête GET peut déclencher une action sur le serveur, comme la suppression d'une ressource.
-->

Les données envoyées via GET peuvent être modifiées facilement par un utilisateur.
-->non

Les requêtes GET sont souvent enregistrées dans les journaux de serveur web.
-->oui

La méthode GET est souvent utilisée pour soumettre des formulaires lorsque les données ne sont pas sensibles.
-->oui

Les requêtes GET sont automatiquement répétées en cas de perte de connexion.
-->non

Les données envoyées via GET sont automatiquement chiffrées par HTTPS.
-->oui

Il est recommandé d'utiliser GET pour les requêtes qui ne doivent pas être répétées.
-->oui
Les requêtes GET ne peuvent pas inclure de paramètres de requête dans l'URL.
-->oui

Les données envoyées par GET sont généralement visibles dans l'historique du navigateur.

-->oui
Les requêtes GET peuvent être utilisées pour obtenir le contenu d'une page web.
-->non

Les requêtes GET peuvent être utilisées pour envoyer des données à un serveur.
-->oui
*/